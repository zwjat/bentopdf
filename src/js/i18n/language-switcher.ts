import {
    supportedLanguages,
    languageNames,
    getLanguageFromUrl,
    changeLanguage,
} from './i18n';

export const createLanguageSwitcher = (): HTMLElement => {
    return null;
};

export const injectLanguageSwitcher = (): void => {
    
};
